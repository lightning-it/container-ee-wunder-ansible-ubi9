# ---------------------------------------------------------------------------
# ee-wunder-ansible-ubi9 – Best-practice Python requirements
# Target: Ansible Execution Environment for AAP + ansible-navigator
# Policy: pinned core, safe-minimum extras, VMware + optional Vault.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Core runtime (pin to 2.18.x series)
# ---------------------------------------------------------------------------
ansible-core==2.18.17
ansible-runner==2.4.3

# ---------------------------------------------------------------------------
# Optional: Hashi Vault client (only used if vault lookups are enabled)
# ---------------------------------------------------------------------------
hvac==2.4.0

# ---------------------------------------------------------------------------
# VMware Python SDK required by community.vmware modules
# (pyVim import comes from pyvmomi)
# ---------------------------------------------------------------------------
pyvmomi==9.1.0.0

# ---------------------------------------------------------------------------
# HTTP/TLS stack (helps avoid runtime surprises in minimal images)
# - requests/urllib3: API calls (vCenter, REST endpoints, etc.)
# - cryptography: TLS / cert-related functionality used by many deps
# - certifi: CA bundle consistency in containers
# ---------------------------------------------------------------------------
requests==2.33.0
urllib3==2.7.0
cryptography==48.0.1
certifi==2026.5.20

# ---------------------------------------------------------------------------
# Common parsing utilities used widely by Ansible plugins/collections
# (often already pulled indirectly, but pinning avoids drift)
# ---------------------------------------------------------------------------
PyYAML==6.0.3
jmespath==1.1.0
packaging==26.2
dnspython==2.8.0
netaddr==1.3.0
kubernetes==36.0.2
